import { StyleSheet } from "react-native";
import { primitives } from "styles";

export const styles = StyleSheet.create({
  text: {
    fontSize: primitives.font.body,
    textDecorationLine: "underline",
    fontWeight: "bold",
    cursor: "pointer",
  },
});
