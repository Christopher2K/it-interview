import { StyleSheet } from "react-native";
import { primitives } from "styles";

export const styles = StyleSheet.create({
  root: {
    fontSize: primitives.font.title,
    marginBottom: 20,
    fontWeight: "bold",
  },
});
